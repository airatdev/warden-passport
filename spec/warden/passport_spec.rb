require 'spec_helper'

describe Warden::Passport do
  it 'has a version number' do
    expect(Warden::Passport::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
